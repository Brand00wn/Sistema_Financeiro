@model Financeiro.Models.Curso

@{
    ViewBag.Title = "Cursos";
}

<h2>Deletar Curso</h2>

<h3>Tem certeza que deseja deletar este curso?</h3>
<div>

    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Tipo.Tipo1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Tipo.Tipo1)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NomeCurso)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NomeCurso)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="button" class="btn btn-default" value="Não" onclick="location.href='@Url.Action("Index", "Curso")'" />
            <input type="submit" value="Sim, delete!" class="btn btn-default" />
        </div>
    }
</div>
